/**
 * Stands in for the player's file so this package compiles on its own. The adapter replaces
 * it with whatever the player wrote.
 */
public class Solution {
    public static String greet(String name) {
        return "Ola, " + name + "!";
    }
}
